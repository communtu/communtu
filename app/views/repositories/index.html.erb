<h1><%=_('Repositories')%></h1>

<table>
  <tr>
  </tr>

<% for repository in @repositories %>
  <tr>
    <td><%= link_to _('Anzeigen'), repository %></td>
    <td><%= link_to _('Bearbeiten'), edit_repository_path(repository) %></td>
    <td><%= link_to _('Löschen'), repository, :confirm => _('Bist du sicher?'), :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to _('Neues Repository'), new_repository_path %>
