<h1>New info</h1>

<% form_for(@info) do |f| %>
  <%= f.error_messages %>

<p>Bitte HTML-formatieren! &lt;h1>Überschrift&lt;/h1>&lt;br/>Laberlaber</p>
  <p>
    <%= label_tag(:header) %><br />
    <%= text_field_tag(:header) %>
  </p>
  <p>
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </p>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', infos_path %>
