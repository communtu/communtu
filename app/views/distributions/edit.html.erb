<h1>Distribution bearbeiten</h1>

<%= error_messages_for :distribution %>

<% form_for(@distribution) do |f| %>
  <p>
    <b>Name</b><br />
    <%= f.text_field :name %>
  </p>

  <p>
    <b>Url</b><br />
    <%= f.text_field :url %>
  </p>

  <p>
    <b>Beschreibung</b><br />
    <%= f.text_area :description %>
  </p>
  <p>
    <%= f.submit "Speichern" %>
  </p>
<% end %>

<%= link_to 'Anzeigen', @distribution %> |
<%= link_to 'Zurück', distributions_path %>
