<% if logged_in? %>
<div class='inner0'>
<span class='headline'><%=_("Verfügbare Distributionen")%></span><br/>
<% for distribution in @distributions %>
 <%= dist_show distribution, "inner1" %>
<% end %>
<% if is_admin? %>
<%= link_to _('Neue Distribution'), new_distribution_path %>
<% end %>

</div>
<% else %>
<div class="inner0">
<p><%=_("Du bist momentan nicht eingeloggt. Bitte klicke oben auf Anmelden.")%></p>
</div>
<% end %>