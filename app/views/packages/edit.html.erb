<span class="headline">Paket bearbeiten</span>

<%= error_messages_for :package %>
<%= render_flash %>

<div class="inner0">
<% form_for(:package, @package, :url => ("/packages/update/"+@package.id.to_s)) do |f| %>
  <p>
    <b>Name</b><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <b>Version</b><br />
    <%= f.text_field :version %>
  </p>
  <p>
    <b>Weblinks</b><br />
    <%= f.text_area :urls, :rows =>3 %>
  </p>
  <p>
    <%= f.submit "Speichern" %>
  </p>
<% end %>

<%= link_to 'Anzeigen', distribution_package_path(@package.distribution_id, @package) %> |
<%= link_to 'Zurück', distribution_package_path(@package.distribution_id, @package) %>
</div>