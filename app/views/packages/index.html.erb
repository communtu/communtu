<% if logged_in? %>
<%= render :partial => "packagelist", :locals => { :packages => @packages, :distribution => @distribution, :groups => @groups } %>
<% else %>
<div class="inner0">
<p><%=t(:view_packages_index_0)%></p>
</div>
<% end %>