<% if @failed_packages.empty? %>

    <p>Alle Packete wurden erfolgreich migriert.</p>

<% else %>

    <p>Folgende Buendel konnten nicht migriert werden, da ein oder mehrere Pakete des Buendels, 
    in der Zieldistribution: <%= @distribution.name %>, nicht mehr vorhanden sind.</p>
    
    <table width="100%" cellspacing="0">
    <tr>
        <th align="left" class="packageList">Name</th>
    </tr>
    <% @failed_packages.each do |package| %>
        
        <% css=cycle("packageList0", "packageList1") %>
        <tr>
        <td class="<%= css %>"><%= link_to package.name, package_path(package) %></td>
        </tr>
    <% end %>
    </table>

<% end %>

<% if not @double_packages.empty? %>

    <p>Folgende Buendel konnten nicht migriert werden, da ein Buendel mit dem selben Namen schon in der, 
    der Zieldistribution: <%= @distribution.name %>, existiert.</p>
    
    <table width="100%" cellspacing="0">
    <tr>
        <th align="left" class="packageList">Name</th>
    </tr>
    <% @double_packages.each do |package| %>
        
        <% css=cycle("packageList0", "packageList1") %>
        <tr>
        <td class="<%= css %>"><%= link_to package.name, package_path(package) %></td>
        </tr>
    <% end %>
    </table>
    
<% end %>

<%= link_to('Weiter', @backlink) %>
