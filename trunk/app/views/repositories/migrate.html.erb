<h1><%=t(:migrate_repository)%></h1>
<%= form_tag(:action => 'finish_migrate', :id => @repository.id) %>
<%=t(:view_repositories_migrate_1)%><%= @repository.name %><%=t(:view_repositories_migrate_2)%>
<p>
<%= collection_select(:distribution, :id, Distribution.find(:all),:id, :name) %>
<%= submit_tag t(:migrate) %>
</p>
</form>